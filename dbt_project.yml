name: 'consulting_firm_dw'
version: '1.0.0'
config-version: 2

profile: 'consulting_firm_dw'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  consulting_firm_dw:
    staging:
      +materialized: view
      +schema: staging
    dimensions:
      +materialized: table
      +schema: warehouse
    marts:
      +materialized: table
      +schema: warehouse
